<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.beautyinside">

    <!-- 🔐 필요한 권한들 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Android 12 이하 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Android 13 이상 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Beautyinside"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Naver Map -->
        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="3ucsetgjda" />

        <!-- ⭐ 앱 첫 화면 -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 기본 화면 -->
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ui.ResultActivity" />
        <activity android:name=".PredictActivity" />

        <!-- 리뷰 / 검색 -->
        <activity android:name=".ReviewMoreActivity" />
        <activity android:name=".SimilarReviewActivity" />
        <activity android:name=".SearchResultActivity" />

        <!-- 병원 상세 Activity들 -->
        <activity android:name=".hospital.PopHospitalDetailActivity" />
        <activity android:name=".hospital.TsHospitalDetailActivity" />
        <activity android:name=".hospital.AmondHospitalDetailActivity" />
        <activity android:name=".hospital.BabaHospitalDetailActivity" />
        <activity android:name=".hospital.ClasyHospitalDetailActivity" />
        <activity android:name=".hospital.DmHospitalDetailActivity" />
        <activity android:name=".hospital.DressHospitalDetailActivity" />
        <activity android:name=".hospital.EsHospitalDetailActivity" />
        <activity android:name=".hospital.HitHospitalDetailActivity" />
        <activity android:name=".hospital.MadeHospitalDetailActivity" />
        <activity android:name=".hospital.MarbleHospitalDetailActivity" />
        <activity android:name=".hospital.MindHospitalDetailActivity" />
        <activity android:name=".hospital.MlHospitalDetailActivity" />
        <activity android:name=".hospital.NanaHospitalDetailActivity" />
        <activity android:name=".hospital.OnairHospitalDetailActivity" />
        <activity android:name=".hospital.RuhoHospitalDetailActivity" />
        <activity android:name=".hospital.ThankyouHospitalDetailActivity" />
        <activity android:name=".hospital.WonderHospitalDetailActivity" />
        <activity android:name=".hospital.YellowHospitalDetailActivity" />
        <activity android:name=".hospital.YounoHospitalDetailActivity" />

        <!-- 📸 FileProvider (사진 파일 처리용, 삭제 금지) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
